#include <stdio.h>
#include "main.h"

/**
 * main - Calling _puts() to print string to standard output
 *
 * Function: _puts()
 *
 * Return: 0
 */

int main(void)
{
	char str[] = "Hello, World!";

	_puts(str);
	_puts("C iS fun");

	return (0);
}
