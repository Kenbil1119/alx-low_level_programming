#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen - To return the length of a string
 *
 * @s: character Parameter to get it's size
 *
 * Return: size of string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
