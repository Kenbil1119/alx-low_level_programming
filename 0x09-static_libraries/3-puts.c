#include <stdio.h>
#include "main.h"

/**
 * _puts - Invoke puts()
 *
 * @s: Character parameter that get string
 *
 * Function - puts(): Puts a string from 's' to the standard
 *				output with a new line
 */

 void _puts(char *s)
{
	puts(s);
}
