#include <stdio.h>
#include "main.h"

/**
 * main - Invoke _puts_recursion()
 *
 * Function: _puts_recursion()
 *
 * main: Print string to the standard output with a new line
 *
 * Return: 0
 */

int main(void)
{
	_puts_recursion("Puts with recursion");

	return (0);
}
