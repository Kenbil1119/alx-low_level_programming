#include "main.h"

/**
 * main - Call print_trianlge() to print triangle of
 *		specified number(size) to the screen
 *
 * Return: 0
 */

int main(void)
{
	print_triangle(10);
	print_triangle(4);
	print_triangle(0);
	print_triangle(-5);

	return (0);
}
