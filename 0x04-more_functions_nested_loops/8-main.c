#include "main.h"

/**
 * main - Invoke print_square() to print a square to
 *			the screen.
 *
 * Return: 0
 */

int main(void)
{
	print_square(5);
	print_square(4);
	print_square(0);

	return (0);
}
