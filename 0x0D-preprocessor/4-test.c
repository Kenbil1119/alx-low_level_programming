#include "main.h"
#include "4-sum.h"

/**
 * main - Invoke a macro "SUM" which compute the sum of two number
 *
 * Return: 0
 */

int main(void)
{
	int sum = SUM(98, 10);

	printf("sum of 98 & 10 is: %i\n", sum);

	return (0);
}
