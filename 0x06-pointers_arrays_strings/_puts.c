#include <stdio.h>
#include "main.h"

/**
 * _puts - Call 'puts()'
 *
 * @str: pointer parameter, pointing to a string
 *
 * _puts: Printing string to the standard output
 */

void _puts(char *str)
{
	puts(str);
}
