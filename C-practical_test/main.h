#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include "date.h"

int set_date(int date, int month, int year);

#endif

/* Header file to include a custom Header file: "date.h" and other built-in header file */
